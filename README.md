# 🐚 Delta Shell - Простой и понятный командный интерфейс

Delta Shell - это простая и понятная командная оболочка (shell) для Linux, которая поможет вам работать с файлами и директориями, даже если вы только начинаете изучать программирование!

## 🎯 Для кого этот проект?

Этот проект создан для:
- **Детей и начинающих** - простой и понятный интерфейс
- **Студентов** - изучение работы с командной строкой
- **Всех, кто хочет** - заменить множество маленьких утилит Linux одной программой

## ✨ Что умеет Delta Shell?

### 📁 Работа с директориями
- `ls` или `dir` - посмотреть, что находится в папке
- `cd` - перейти в другую папку
- `pwd` - узнать, где вы сейчас находитесь
- `mkdir` - создать новую папку
- `rmdir` - удалить папку

### 📄 Работа с файлами
- `cat` - прочитать содержимое файла
- `touch` - создать новый пустой файл
- `rm` - удалить файл
- `cp` - скопировать файл
- `mv` - переместить или переименовать файл

### 🔍 Поиск и фильтрация
- `grep` - найти текст в файле
- `find` - найти файлы по имени
- `head` - показать начало файла
- `tail` - показать конец файла
- `wc` - посчитать строки, слова и символы

### 🛠️ Служебные команды
- `echo` - вывести текст
- `history` - посмотреть историю команд
- `clear` или `cls` - очистить экран
- `help` - показать справку
- `exit_de` - выйти из shell

### 💻 И еще больше!
Delta Shell также может запускать любые программы из вашей системы:
- `python` - запустить Python
- `git` - работать с Git
- `gcc` - компилировать программы
- И любые другие программы Linux!

## 🚀 Как начать использовать?

### Установка

1. Убедитесь, что у вас установлен Python 3.7 или новее
2. Скачайте проект
3. Запустите shell:

```bash
python shell.py
```

### Первые шаги

1. **Посмотрите, где вы находитесь:**
   ```
   pwd
   ```

2. **Посмотрите, что есть в текущей папке:**
   ```
   ls
   ```

3. **Создайте новый файл:**
   ```
   touch test.txt
   ```

4. **Напишите что-то в файл (используя внешние команды):**
   ```
   echo "Привет, мир!" > test.txt
   ```

5. **Прочитайте файл:**
   ```
   cat test.txt
   ```

6. **Посмотрите справку:**
   ```
   help
   ```

## 📚 Структура проекта

```
fastfetch/
├── shell.py              # Главный файл - запускает shell
├── parser.py             # Разбирает команды на части
├── commands/             # Все команды shell
│   ├── base.py          # Диспетчер команд
│   ├── dir_cmd.py       # Команды для директорий
│   ├── file_cmd.py      # Команды для файлов
│   ├── search_cmd.py    # Команды для поиска
│   └── external_cmd.py  # Запуск внешних программ
└── components/           # Вспомогательные компоненты
    ├── dir_utility.py   # Работа с директориями
    ├── history.py        # История команд
    └── console_output.py # Красивый вывод
```

## 🎓 Примеры использования

### Пример 1: Работа с файлами
```bash
# Создать файл
touch myfile.txt

# Написать в файл (используя echo из системы)
echo "Привет!" > myfile.txt

# Прочитать файл
cat myfile.txt

# Скопировать файл
cp myfile.txt backup.txt

# Удалить файл
rm myfile.txt
```

### Пример 2: Работа с директориями
```bash
# Создать новую папку
mkdir myproject

# Перейти в папку
cd myproject

# Посмотреть, где вы находитесь
pwd

# Вернуться назад
cd ..

# Удалить папку
rmdir myproject
```

### Пример 3: Поиск
```bash
# Найти текст в файле
grep "привет" file.txt

# Найти все файлы .txt
find "*.txt"

# Показать первые 5 строк файла
head -n 5 file.txt

# Показать последние 10 строк файла
tail -n 10 file.txt
```

## 💡 Полезные советы

1. **Используйте стрелки вверх/вниз** - для просмотра истории команд
2. **Используйте стрелки влево/вправо** - для редактирования команды
3. **Нажмите Tab** - для автодополнения (если доступно)
4. **Используйте `help`** - если забыли команду
5. **Используйте `history`** - чтобы посмотреть все команды, которые вы вводили

## 🔧 Технические детали

- **Язык:** Python 3.7+
- **Зависимости:** Только стандартная библиотека Python
- **Платформа:** Linux (также может работать на macOS)

## 🤝 Вклад в проект

Если вы хотите улучшить проект:
1. Добавьте новые команды в папку `commands/`
2. Улучшите документацию
3. Исправьте ошибки
4. Сделайте код еще понятнее!

## 📝 Лицензия

Этот проект создан для обучения и может использоваться свободно.

## 🎉 Наслаждайтесь использованием!

Delta Shell создан, чтобы сделать работу с командной строкой простой и понятной. Надеемся, вам понравится!

---

**Помните:** Будьте осторожны при удалении файлов и директорий. Всегда проверяйте, что вы удаляете правильные вещи!

